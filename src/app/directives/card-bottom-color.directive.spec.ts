import { CardBottomColorDirective } from './card-bottom-color.directive';

describe('CardBottomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardBottomColorDirective();
    expect(directive).toBeTruthy();
  });
});
